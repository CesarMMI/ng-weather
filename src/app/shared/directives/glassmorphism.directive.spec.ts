import { GlassmorphismDirective } from './glassmorphism.directive';

describe('GlassmorphismDirective', () => {
  it('should create an instance', () => {
    const directive = new GlassmorphismDirective();
    expect(directive).toBeTruthy();
  });
});
